python3 run.py --path "Build a React-based Monster Squad setting that allows me to add multiple monsters to a single encounter and track hp for all of them.  it should allow me to clear monsters as they die and add them again for the next battle it would be great if we could point it at a pdf of the adventure and select a section so the monsters and npcs are created automatically by scene.  we need a function that maps our JSON entries to audio stations It should also pulls from creatures.json and suggests a Pocket Bard audio vibe." --name "DMVault_MonsterSquad"