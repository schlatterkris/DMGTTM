version: "0.4.0"
vars: {}
graph:
  id: dm_vault_builder
  nodes:
    - id: "architect_01"
      type: "agent"
      config:
        name: "gemini-1.5-pro"
        provider: openai
        role: |-
          Chief Executive Officer
        params:
          temperature: 0.2
    - id: "coder_01"
      type: "agent"
      config:
        name: "gemini-1.5-pro"
        provider: openai
        role: |-
          Programmer
        params:
          temperature: 0.1
  edges:
    - from: "architect_01"
      to: "coder_01"
task: >
  Build 'The Ultimate DM Vault' using Next.js.
  Data is in /public/data/.
  Use existing globals.css for a parchment UI.
  Include logic to verify Gods in gods.json match Planes in planes.json.
